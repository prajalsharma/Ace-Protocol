# ACE Backend

Fastify + SQLite backend for the ACE Protocol MVP.

Current responsibilities:

- signed wallet sessions
- persistent treasury state
- scheduled payment persistence
- deterministic execution routing
- x402 machine-payment simulation
- policy-driven payment execution

Run locally with:

```bash
npm run backend:dev
```
